namespace Infrastructure.Constants;

public static class SettingsSectionNameConstants
{
    public const string JwtSettings = nameof(JwtSettings);
    public const string DataGeneratorSettings = nameof(DataGeneratorSettings);
    public const string OpenExchangeRatesSettings = nameof(OpenExchangeRatesSettings);
}